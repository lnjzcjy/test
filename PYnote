# this text is for python note
# what must import !
import numpy as np 
import matplotlib.pyplot as mpl
import sklearn.linear_model.LinearRegression as lin_reg
from sklearn.linear_model import LinearRegression # that is another way to import LR
# random number 
np.random.randn(100,1) # that means product 100*1 random number 
# easiest plot
mpl.plot(X,y,'b.')      # that means plot X cross y with blue .  also r- g* 
# easiest use LR each model have function fit predict  and mumber intercept_ coef_
lin_reg.fit(X,y)
lin_reg.intercept_
lin_reg.coef_
lin_predict(some_number)
